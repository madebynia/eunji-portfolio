export function SiteFooter() {
  return (
    <footer className="site-footer">
      <div className="site-footer-inner">
        <p>© {new Date().getFullYear()} EUNJI</p>
        <p>Complex work → clearer systems → things I build.</p>
      </div>
    </footer>
  );
}
